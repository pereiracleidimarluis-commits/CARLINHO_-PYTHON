 Respostas Completas — Lista de Exercícios Python
 
Cleidimar Luiz Pereira ✅
 
 
 
📦 Parte 1: Listas ( list )
 
✅ Questão 1
 
python
  
frutas = ["maçã", "banana", "uva", "laranja", "manga"]
print("Primeira fruta:", frutas[0])
print("Última fruta:", frutas[-1])
 
 
✅ Questão 2
 
python
  
numeros = [10, 20, 30, 40]
numeros.append(50)
numeros.remove(20)
print("Tamanho da lista:", len(numeros))
 
 
✅ Questão 3
 
python
  
notas = []
for i in range(3):
    nota = float(input(f"Digite a nota {i+1}: "))
    notas.append(nota)
print("Lista de notas:", notas)
 
 
✅ Questão 4
 
python
  
linguagens = ["Python", "C++", "Java", "JavaScript"]
busca = input("Digite uma linguagem de programação: ")
if busca in linguagens:
    print(f"{busca} está na lista! ✅")
else:
    print(f"{busca} NÃO está na lista! ❌")
 
 
✅ Questão 5
 
python
  
valores = [15, 30, 45, 60]
print("Soma total:", sum(valores))
 
 
 
 
🔄 Parte 2: Laço de Repetição  for 
 
✅ Questão 6
 
python
  
for numero in range(1, 16):
    print(numero)
 
 
✅ Questão 7
 
python
  
numeros = [2, 5, 8, 11, 14]
for n in numeros:
    print(f"Dobro de {n} = {n * 2}")
 
 
✅ Questão 8
 
python
  
numero = int(input("Digite um número inteiro: "))
for i in range(1, 11):
    print(f"{numero} x {i} = {numero * i}")
 
 
✅ Questão 9
 
python
  
idades = [12, 18, 25, 10, 30, 15, 22]
contador = 0
for idade in idades:
    if idade >= 18:
        contador += 1
print(f"Quantidade de pessoas maiores de idade: {contador}")
 
 
✅ Questão 10
 
python
  
for n in range(1, 21):
    if n % 2 == 0:
        print(n)
 
 
 
 
🔁 Parte 3: Laço de Repetição  while 
 
✅ Questão 11
 
python
  
contador = 10
while contador >= 1:
    print(contador)
    contador -= 1
print("Fim da contagem!")
 
 
✅ Questão 12
 
python
  
senha_correta = "python123"
senha_digitada = ""
while senha_digitada != senha_correta:
    senha_digitada = input("Digite a senha: ")
print("✅ Senha correta! Acesso permitido.")
 
 
✅ Questão 13
 
python
  
soma = 0
while True:
    numero = int(input("Digite um número (0 para parar): "))
    if numero == 0:
        break
    soma += numero
print(f"Soma total: {soma}")
 
 
✅ Questão 14
 
python
  
numeros = []
while True:
    valor = int(input("Digite um número inteiro (negativo para parar): "))
    if valor < 0:
        break
    numeros.append(valor)
print("Lista digitada:", numeros)
 
 
✅ Questão 15
 
python
  
valor = float(input("Digite o valor do saque: R$ "))
while valor <= 0:
    print("❌ Erro! O valor deve ser maior que zero.")
    valor = float(input("Digite o valor do saque novamente: R$ "))
print(f"✅ Saque de R$ {valor:.2f} autorizado!")
 
 
 
 
🔀 Parte 4: Seleção com  match-case 
 
✅ Questão 16
 
python
  
dia = int(input("Digite um número de 1 a 7: "))
match dia:
    case 1: print("Domingo")
    case 2: print("Segunda-feira")
    case 3: print("Terça-feira")
    case 4: print("Quarta-feira")
    case 5: print("Quinta-feira")
    case 6: print("Sexta-feira")
    case 7: print("Sábado")
    case _: print("Dia inválido!")
 
 
✅ Questão 17
 
python
  
n1 = float(input("Digite o primeiro número: "))
n2 = float(input("Digite o segundo número: "))
op = input("Digite a operação (+, -, *, /): ")

match op:
    case "+": print(f"Resultado: {n1 + n2}")
    case "-": print(f"Resultado: {n1 - n2}")
    case "*": print(f"Resultado: {n1 * n2}")
    case "/":
        if n2 != 0:
            print(f"Resultado: {n1 / n2}")
        else:
            print("❌ Não é possível dividir por zero!")
    case _: print("❌ Operação inválida!")
 
 
✅ Questão 18
 
python
  
codigo = input("Digite o código do produto: ")
match codigo:
    case "A1" | "A2": print("Categoria: Eletrônicos")
    case "B1" | "B2": print("Categoria: Vestuário")
    case "C1": print("Categoria: Alimentos")
    case _: print("Código desconhecido")
 
 
 
 
🧠 Parte 5: Questões Integradas (Desafios)
 
✅ Questão 19
 
python
  
comandos = ["iniciar", "pausar", "invalido", "parar"]
for cmd in comandos:
    match cmd:
        case "iniciar": print("Sistema iniciando...")
        case "pausar": print("Sistema pausado.")
        case "parar": print("Sistema desligado.")
        case _: print("Comando não reconhecido.")
 
 
✅ Questão 20
 
python
  
compras = []
while True:
    produto = input("Digite um produto (ou 'sair' para encerrar): ")
    if produto.lower() == "sair":
        break
    compras.append(produto)

print("\n🛒 Lista de Compras:")
for indice, item in enumerate(compras, start=1):
    print(f"Item {indice}: {item}")
 
 
